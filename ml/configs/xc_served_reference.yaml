task: xc

data:
  source: database
  database_url_env: ML_DATABASE_URL
  table: glideator_fs.features_with_target
  start_date: "2021-01-01"
  max_site_id: 250
  split_strategy: temporal
  train_end: "2023-12-31"
  eval_start: "2024-01-01"
  eval_end: "2024-12-31"
  require_known_eval_sites: true
  site_features:
    - latitude
    - longitude
    - altitude

evaluation:
  benchmark_id: xc-temporal-2024-v1

reference:
  name: served-production-onnx
  onnx_path: ../backend/app/models/model.onnx

artifact:
  output_dir: outputs/xc/served-reference

tracking:
  enabled: true
  experiment_name: glideator-xc
  tracking_uri_env: MLFLOW_TRACKING_URI
  default_tracking_uri: sqlite:///mlruns/mlflow.db
