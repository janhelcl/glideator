# Redirect individual site llms to backend (llms.txt is generated at build time)
/llms/sites/* https://glideator-web.onrender.com/llms/sites/:splat 302

# Redirect API requests to backend
/api/* https://glideator-web.onrender.com/:splat 200

# Redirect MCP requests to backend
/mcp/* https://glideator-web.onrender.com/mcp/:splat 200

# SPA fallback - serve index.html for all other routes
/* /index.html 200

